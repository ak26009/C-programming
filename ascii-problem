WPA to check weather the input is a digit or alphabet , print the ascii if it is a digit

#include <stdio.h>

int main() {
    char a ;
    printf("Enter the value of a = ");
    scanf("%c",&a);
    
    if((a>48)&&(a<57)){
        printf("The value of the digit is %d \n",a);
    }else{
        printf("It is a character\n");
    }

    return 0;
}
