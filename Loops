Basic Program for While loop

// in this happy birthday is print until and unless the value of i iss 4  

#include<stdio.h>

int main(){
    
    int i = 0 ;
    while(i<4){
        printf("Happy Birthday!\n");
        i = i+1;             // here we can also use i++ increment operator also. 
                             // when the i value reaches to 4 it get out of the loop and the program end 
    }
    
    return 0;
}


Basic practice code in while loop
        FIRST
#include <stdio.h>

// from here we will get 0 to 10

int main() {
    int i = 0;
    while(i<=10){
        printf("%d \n",i);
        i++;
    }
    
    // from here we will get 10 to 0
    i = 10;
    while(i>=0){
        printf("%d \n",i);
        i--;
    }
    return 0;
}

      SECOND
// In this code we have used += and whileloop 
// It will add all the positive number from 1 to infinite, if we enter 0 it will break the loop and the execution will stop 
#include <stdio.h>

int main() {
   int sum = 0 , number ;
   
   printf("Enter the positive value to add and 0 to stop\n");
   
   while(1){
       printf("Enter the number = ");
       scanf("%d",&number);
       
       if(number==0){
           printf("You have entered 0\n");
           break;
       }
       if(number>0){
           sum +=number;
       }
    //   i++;
   }
    printf("The Value of positive number is %d",sum);
    return 0;
}





Do-While Loop
// In do while loop the the do BLOCK is run then the while condition is checked  
#include <stdio.h>

// this code is to write the natural numbers by taking input from the user 

int main() {
    int n, i = 1;
    printf("Enter the number = ");
    scanf("%d",&n);
    
    do{
        printf("the value on n is %d\n",i);
        i++;
    }while(i<=n);
    return 0;
}




FOR Loop 
Syntax for( initialization ; condition ; increment ){
            }        
// In for loop the intaitialization and condition statement is checked first and it runs first after that the increment happens 



Ex To write natural numbers using for loop and taking inpt from the user

#include <stdio.h>

int main() {
    int n ;
    printf(" Enter the Vlaue for n numbers ");
    scanf("%d",&n);
    for(int i = 1; i<=n ; i++){
        printf("The value of i is %d\n",i);
    }
    return 0;
}


Ex To write a code for decrement operator i--
// In this code the for loop works only till non zero value only  i.e 1 after that the execution stops 
// This code will execute until i becomes 0 
#include <stdio.h>

int main() {

    for(int i=6; i ; i--){
        printf("%d\n",i);
    }
    return 0;
}

Output : 
6
5
4
3
2
1


Ex To print natural numbers in a descending order 
// Taking inputfrom the user for natural numbers 

#include <stdio.h>

int main() {
    int i;
    printf("enter the value ");
    scanf("%d",&i);
    for(i; i ; i--){
        printf("%d\n",i);
    }
    return 0;
}




